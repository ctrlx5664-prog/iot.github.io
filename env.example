## Environment variables template (copy to your hosting provider settings)

# Neon Postgres connection string (from Neon project dashboard)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Default admin account (created on first run)
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme
AUTH_EMAIL=admin@yourdomain.com

# Email (for 2FA verification codes) - Using Resend (free: 100 emails/day)
# Get your API key from https://resend.com
RESEND_API_KEY=re_xxxxxxxxx
# The email address to send from (must be verified in Resend)
EMAIL_FROM=noreply@yourdomain.com

# CORS (set this to your frontend URL when frontend is hosted elsewhere)
# You can set multiple origins separated by commas.
CORS_ORIGIN=https://your-frontend.com

# Local server
PORT=5000

# Frontend (optional)
# Enable WebSockets (only works when running the Node server, not Netlify Functions)
VITE_ENABLE_WS=false

# Frontend (when hosted elsewhere)
VITE_API_BASE_URL=https://your-backend.netlify.app
VITE_API_CREDENTIALS=omit

# Home Assistant (for embedded dashboard proxy)
# You can set this as a full URL (recommended), e.g.:
#   HA_BASE_URL=https://ctrlxprod.diogomendes.me
# Or just the hostname (we will assume https):
#   HA_BASE_URL=ctrlxprod.diogomendes.me
HA_BASE_URL=

# Home Assistant Long-Lived Access Token (Profile -> Long-Lived Access Tokens)
# Use a dedicated HA user with minimal permissions if possible.
HA_TOKEN=


